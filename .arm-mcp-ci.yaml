# CI configuration for the vec_dot sample and pipeline scripts.

languages:
  - cpp
  - java

# Performix profiles Java only. Sources under code/java/ are SCP'd to the APX
# host and compiled there with javac before apx_recipe_run.
performix:
  recipe: code_hotspots
  java_dir: code/java
