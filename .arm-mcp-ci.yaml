# CI configuration for the vec_dot sample and pipeline scripts.

languages:
  - cpp

build:
  default: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build -j$(nproc) && ctest --test-dir build --output-on-failure

docker_images:
  - debian:bookworm-slim
